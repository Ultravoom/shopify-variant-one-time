# Shopify shop domain, e.g. myshop.myshopify.com
SHOP=

# Shopify Admin API access token (private app or custom app)
ADMIN_TOKEN=

# SMTP (or comment these and use SendGrid/Mailgun in routes/proxy.js)
SMTP_HOST=
SMTP_USER=
SMTP_PASS=

# (Optional) Port for local dev
PORT=3000
